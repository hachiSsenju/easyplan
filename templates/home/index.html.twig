{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
{# <div class="container mx-auto p-6">
    <h1 class="text-3xl font-bold mb-6">Tableau de bord</h1>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
        <div class="bg-white shadow rounded p-6 text-center">
            <h2 class="text-xl font-semibold">Total Classes</h2>
            <p class="text-4xl font-bold text-blue-600 mt-2">{{ totalSalles }}</p>
        </div>
        <div class="bg-white shadow rounded p-6 text-center">
            <h2 class="text-xl font-semibold">Total Utilisateurs</h2>
            <p class="text-4xl font-bold text-green-600 mt-2">{{ totalUsers }}</p>
        </div>
        <div class="bg-white shadow rounded p-6 text-center">
            <h2 class="text-xl font-semibold">Total Réservations</h2>
            <p class="text-4xl font-bold text-red-600 mt-2">{{ totalReservations }}</p>
        </div>
    </div>

    <div class="bg-gray-100 p-6 rounded shadow">
        {% if user %}
            <p>Connecté en tant que : <strong>{{ user.email }}</strong></p>
            <a href="{{ path('app_logout') }}" class="text-red-500 hover:underline">Se déconnecter</a>
        {% else %}
            <p>Vous n'êtes pas connecté.</p>
            <div class="space-x-4 mt-2">
                <a href="{{ path('app_login') }}" class="text-blue-500 hover:underline">Se connecter</a>
                <a href="{{ path('app_register') }}" class="text-green-500 hover:underline">Créer un compte</a>
            </div>
        {% endif %}
    </div>
</div> #}
 <section class="section dashboard">
      <div class="row">
         <div class="bg-gray-100 p-6 rounded shadow">
        {% if user %}
            <p>Connecté en tant que : <strong>{{ user.email }}</strong></p>
            <a href="{{ path('app_logout') }}" class="text-red-500 hover:underline">Se déconnecter</a>
        {% else %}
            <p>Vous n'êtes pas connecté.</p>
            <div class="space-x-4 mt-2">
                <a href="{{ path('app_login') }}" class="text-blue-500 hover:underline">Se connecter</a>
                <a href="{{ path('app_register') }}" class="text-green-500 hover:underline">Créer un compte</a>
            </div>
        {% endif %}
    </div>
        <!-- Left side columns -->
        <div class="col-lg-12">
          <div class="row">

         

            <div class="col-xxl-4 col-md-6">
              <div class="card info-card sales-card">

                <div class="filter">
                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                    <li class="dropdown-header text-start">
                      <h6>Filter</h6>
                    </li>

                    <li><a class="dropdown-item" href="#">Today</a></li>
                    <li><a class="dropdown-item" href="#">This Month</a></li>
                    <li><a class="dropdown-item" href="#">This Year</a></li>
                  </ul>
                </div>

                <div class="card-body">
                  <h5 class="card-title">Total <span>| Utilisateurs</span></h5>

                  <div class="d-flex align-items-center">
                    <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                      <i class="bi bi-people"></i>
                    </div>
                    <div class="ps-3">
                      
                      <span class="text-success small pt-1 fw-bold">{{ totalUsers }}</span> 

                    </div>
                  </div>
                </div>

              </div>
            </div><!-- End Sales Card -->

            <!-- Revenue Card -->
            <div class="col-xxl-4 col-md-6">
              <div class="card info-card revenue-card">

                <div class="filter">
                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                    <li class="dropdown-header text-start">
                      <h6>Filter</h6>
                    </li>

                    <li><a class="dropdown-item" href="#">Today</a></li>
                    <li><a class="dropdown-item" href="#">This Month</a></li>
                    <li><a class="dropdown-item" href="#">This Year</a></li>
                  </ul>
                </div>

                <div class="card-body">
                  <h5 class="card-title">Total <span>| Salle</span></h5>

                  <div class="d-flex align-items-center">
                    <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                      <i class="bi bi-person bg-blue"></i>
                    </div>
                    <div class="ps-3">
                    
                      <span class="text-success small pt-1 fw-bold">{{totalSalles}}</span>

                    </div>
                  </div>
                </div>

              </div>
            </div><!-- End Revenue Card -->

            <!-- Customers Card -->
            <div class="col-xxl-4 col-xl-12">

              <div class="card info-card customers-card">

                <div class="filter">
                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                    <li class="dropdown-header text-start">
                      <h6>Filter</h6>
                    </li>

                    <li><a class="dropdown-item" href="#">Today</a></li>
                    <li><a class="dropdown-item" href="#">This Month</a></li>
                    <li><a class="dropdown-item" href="#">This Year</a></li>
                  </ul>
                </div>

                <div class="card-body">
                  <h5 class="card-title">Total <span>| Reservations</span></h5>

                  <div class="d-flex align-items-center">
                    <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                      <i class="bi bi-person"></i>
                    </div>
                    <div class="ps-3">
                     
                      <span class="text-danger small pt-1 fw-bold">{{totalReservations}}</span> 

                    </div>
                  </div>

                </div>
              </div>

            </div><!-- End Customers Card -->

            <!-- Reports -->
            <div class="col-12">
              <div class="card">

                <div class="filter">
                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                    <li class="dropdown-header text-start">
                      <h6>Filter</h6>
                    </li>

                    <li><a class="dropdown-item" href="#">Today</a></li>
                    <li><a class="dropdown-item" href="#">This Month</a></li>
                    <li><a class="dropdown-item" href="#">This Year</a></li>
                  </ul>
                </div>

               

              </div>
            </div><!-- End Reports -->

            <!-- Recent Sales -->
           

            <!-- Top Selling -->
 

          </div>
        </div><!-- End Left side columns -->

        <!-- Right side columns -->
      

      </div>
    </section>
{% endblock %}
